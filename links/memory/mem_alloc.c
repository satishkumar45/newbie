#include<stdio.h>
#include<stdlib.h>
int main()
{
   printf("enter the things which composed");
   int a;
   struct node {
       int a;
       struct node base;

   };
   base=(struct node *)malloc(sizeof(node));

}
